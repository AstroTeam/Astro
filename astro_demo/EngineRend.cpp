#include "main.hpp"
//#include "engine.cpp"
#include "SDL/SDL.h"
//#include "SDL/SDL_image.h"
#include <string>



void EngineRend::render(void *sdlSurface) {
	/*//SDL_Surface *screen=(SDL_Surface *)sdlSurface;
	//SDL_Surface *background = SDL_LoadBMP("hello.bmp");
	SDL_Surface *screen=(SDL_Surface *)sdlSurface;
	static bool first=true;
	if ( first ) {
		first=false;
		// set black as transparent for the root console
		// so that we only blit characters
		SDL_SetColorKey(screen,SDL_SRCCOLORKEY,0);
		if (engine.map->initFlag == true)
		{
			SDL_Surface *map = (SDL_Surface*)engine.mapcon;
			SDL_SetColorKey(map,SDL_SRCCOLORKEY,0);
		}
	}
	//SDL_BlitSurface(screen,NULL,background,NULL);
	// and copy the result back on screen				
	//SDL_BlitSurface(background,NULL,screen,NULL);	
	//SDL_FreeSurface(background);*/
		
}
	
	
	
